import Config = require("../Config");

export = Main;

/**
 * It may be needed or useful to separate out reusable functions into utilities.
 * If a utility is reusable by multiple areas,  consider adding it to the Areas/Shared/Util
 * folder instead of making it area-specific.
 *
 * Note: anything added to shared should have unit tests for it since it may be used in
 * unintended ways and refactoring would impact multiple areas.
 */
module Main {
}